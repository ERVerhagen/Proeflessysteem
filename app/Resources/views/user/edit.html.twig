{% extends 'base.html.twig' %}

{% block body %}
        {% if is_granted('ROLE_ADMIN') %}
    <h1>User edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success buttonsaligned" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

            <a class="btn btn-warning buttonsaligned" href="{{ path('user_index') }}">Back to the list</a>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger buttonsaligned" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        {% else %}
            <h1>U dient als admin in te loggen</h1>
        {% endif %}
{% endblock %}
