{% extends 'base.html.twig' %}

{% block body %}
    <h1>Inschrijving edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success buttonsaligned" type="submit" value="Save" />
    {{ form_end(edit_form) }}

            <a class="btn btn-warning buttonsaligned" href="{{ path('inschrijving_index') }}">Back to the list</a>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger buttonsaligned" type="submit" value="Delete">
            {{ form_end(delete_form) }}
{% endblock %}
