{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorie edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success buttonsaligned" type="submit" value="Opslaan" />
    {{ form_end(edit_form) }}

            <a class="btn btn-warning buttonsaligned" href="{{ path('categorie_index') }}">Back to the list</a>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger buttonsaligned" type="submit" value="Delete">
            {{ form_end(delete_form) }}
{% endblock %}
