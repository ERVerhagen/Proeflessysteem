{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lessen creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

            <a class="btn btn-warning" href="{{ path('lessen_index') }}">Back to the list</a>
{% endblock %}
